# 業務日報システム 導入手順

## まず、なぜインポートできなかったか

スプレッドシートの **ファイル → インポート** は、**表のデータ**(CSV・Excelなど)を
シートに取り込むための機能です。ここに `.gs` を入れる場所はありません。

GASのコードは **スプレッドシートの中ではなく、その裏側にあるスクリプトエディタ**
という別の場所に置きます。そこへは「拡張機能」から入ります。
そして、そのエディタには **ファイルをアップロードする機能がありません。**
**テキストをコピーして貼り付ける** のが唯一の入れ方です。

つまり、やることはこれだけです。

```
スプレッドシート
  └ 拡張機能 → Apps Script      ← ここを開く
       ├ Code.gs      ← ダウンロードしたファイルの中身を全部コピペ
       └ WebApp       ← 同じくコピペ(HTMLファイルとして作る)
```

---

## 用意するもの

貼り付けるファイルは **2つだけ** です。

| ファイル | エディタでの種類 | エディタでの名前 |
|---|---|---|
| `Code.gs` | スクリプト | `Code`(既定の `コード` のままでも可) |
| `WebApp.html` | HTML | `WebApp` ← **必ずこの名前** |

`.gs` ファイルはただのテキストです。メモ帳・VS Code・テキストエディットなど、
何で開いても構いません。中身を全選択してコピーします。

> 分割版(`S1_Config.gs` 〜 `S6_Print.gs`)は、この `Code.gs` と中身が同じものです。
> 6つに分けたほうが読みやすければそちらでも動きます。貼る手間が少ないのは1ファイル版です。
> **両方を入れると `CFG` の重複エラーになります。** どちらか一方にしてください。

---

## 手順

### 1. スプレッドシートを作る

Googleドライブで新規スプレッドシートを作り、「業務日報」など分かる名前を付けます。
**中身は空のままで構いません。** シートはスクリプトが自動で作ります。

### 2. スクリプトエディタを開く

スプレッドシートのメニューから **拡張機能 → Apps Script**。
新しいタブでエディタが開きます。左側に「ファイル」という欄があり、
`コード.gs` が1つだけある状態です。

### 3. Code.gs を貼る

1. 左の `コード.gs` をクリック
2. エディタ内をクリックして **Ctrl+A**(Macは **⌘+A**)で全選択 → **Delete**
3. ダウンロードした `Code.gs` の中身を全部コピーして貼り付け
4. **Ctrl+S**(⌘+S)で保存

ファイル名は `コード` のままで構いません。気になるなら、ファイル名の右の
「⋮」→ 名前を変更 で `Code` にできます。

### 4. WebApp を貼る

1. 左の「ファイル」の右にある **＋** をクリック
2. 出てきたメニューから **HTML** を選ぶ
3. 名前の入力欄が出るので **`WebApp`** と入力(**`.html` は付けない**)
4. 中に最初から入っている雛形を全選択して削除
5. `WebApp.html` の中身を全部コピーして貼り付け
6. **Ctrl+S** で保存

> ここで名前を間違えると、あとで画面が真っ白になります。
> 左のファイル一覧が `Code.gs` と `WebApp.html` の2つになっていればOKです。

### 5. 設定を書き換える

`Code.gs` の上のほう、「1. 設定」の中を実態に合わせます。

```js
STORE_CODE: 'SPK',                 // 店舗コード。多店舗化のときに効く
STORE_NAME: 'くるむ薬局 札幌店',    // 画面の左上に出る名前
AUTH_MODE: 'select',               // 'select' = 担当プルダウン(既定)
WEEK_START: 6,                     // 6 = 土曜始まり(帳票に合わせる)
BUSINESS_HOURS: '10:00〜20:00',     // 帳票の各日に印字する営業時間
RANGES: {                          // 管理基準。null にすると警告を出さない
  ROOM_TEMP:  [1, 30],   ROOM_HUMID: [30, 70],
  COLD_TEMP:  [2, 8],    COLD_HUMID: null
}
```

書き換えたら **Ctrl+S**。

### 6. setup を実行する

1. エディタ上部の関数選択のプルダウンから **`setup`** を選ぶ
2. 左の **▶ 実行** をクリック

**初回だけ、権限の承認を求められます。** ここが一番つまずくところなので詳しく書きます。

1. 「承認が必要です」→ **権限を確認**
2. Googleアカウントを選ぶ
3. **「このアプリはGoogleで確認されていません」** という警告が出ます
   → 左下の **詳細** をクリック
   → **「(プロジェクト名)（安全ではないページ）に移動」** をクリック
4. **許可** をクリック

自分で書いたスクリプトなので、この警告が出るのが正常です。
ここで止まってしまう人が多いですが、「詳細」を開かないと先に進めません。

実行が終わると、スプレッドシートに4つのシートができています。

- **日報DB** ― 1行 = 1日
- **担当者マスタ** ― ID・氏名・メール・在籍・印影ファイルID
- **管理薬剤師任期** ― ID・担当者ID・氏名・就任日
- **変更履歴** ― 日時・操作・内容・操作者

あわせて、スプレッドシートと同じフォルダに **`業務日報_印影`** というフォルダが作られます。
**このフォルダは誰とも共有しないでください。** スクリプトがオーナー権限で読むので、
共有しなくても印刷物には正しく貼られます。

### 7. デプロイする(画面を公開する)

1. エディタ右上の **デプロイ → 新しいデプロイ**
2. 左の歯車 **「種類の選択」→ ウェブアプリ**
3. 設定を次のようにする

| 項目 | 値 |
|---|---|
| 説明 | 業務日報(なんでも可) |
| 次のユーザーとして実行 | **自分** |
| アクセスできるユーザー | **全員**、または組織内 |

4. **デプロイ** → もう一度 **アクセスを承認**
5. **ウェブアプリのURL** が表示されます。これが入力画面です

URLをコピーしてスマホで開き、ホーム画面に追加しておくと使いやすくなります。

> 「アクセスできるユーザー」を **全員** にしても、URLを知らない人には辿り着けません。
> ただしURLが漏れると誰でも入力できてしまうので、URLはLINE WORKSのトークルームなど
> 内部だけで共有してください。Google Workspace をお使いなら「組織内」が確実です。

### 8. 帳票テンプレートを作る

関数選択で **`buildTemplate`** を選んで実行します。
「帳票テンプレート」シートが、罫線・結合・列幅・表示形式まで整った状態でできます。

**既に「帳票テンプレート」がある場合は消しません。**
`帳票テンプレート_旧20260830_2245` のように名前を変えて退避してから、新しく作ります。
手で直した分が消えることはないので、안心して実行できます。

作ったあと罫線や列幅を微調整したくなったら、**このシートを直接いじって構いません。**
出力時はコピーして値を流し込むだけなので、書式はそのまま印刷物に出ます。

| 関数 | 何をするか |
|---|---|
| `buildTemplate()` | テンプレートを作る(既存は退避) |
| `checkTemplate()` | 形が想定どおりか確認し、各日の書き込み先の行番号を表示 |
| `applyTemplateFormats()` | 温度・湿度のセルに表示形式だけを設定し直す |
| `dumpTemplateLayout()` | **いまのシートの列幅・行の高さを書き出す** |

**`dumpTemplateLayout`** は、シート上で列幅や行高を手で調整したあとに使います。
実行ログに、そのまま `CFG.TEMPLATE` に貼れる形で出ます。

```
■ 列幅
  A列 : 104 px
  B列 : 52 px
  …
■ 合計 : 幅 732 px / 高さ 550 px
  ※ A4横の印刷可能幅はおよそ 1030px 相当（余白0.4インチのとき）

■ CFG.TEMPLATE に貼れる形
    WIDTH: [104, 52, 26, 52, 26, 56, 68, 68, 68, 68, 72, 72],
    ROW_H: { HEAD1: 30, HEAD2: 16, UPPER: 44, LOWER: 28 },
```

日によって行の高さが違う場合は、それも指摘してくれます。

見出しの文言・列幅・行の高さ・単位の表示形式は、すべて `Code.gs` の
**`CFG.TEMPLATE`** にあります。変えたければそこを直して `buildTemplate` を実行し直してください。

### 9. 最初の登録

画面右上のアイコンから、**この順番で** 登録します。

1. **担当者タブ** ― 全員の氏名を追加
2. **管理薬剤師タブ** ― 現在の管理薬剤師を、就任日とあわせて登録
3. **担当者タブ** ― 各行の「未登録」ボタンから印影を登録

**2番を飛ばすと承認ボタンが出ません。** 管理薬剤師が誰か決まっていないと、
誰が承認していいか判断できないためです。過去に遡って交代を登録することもできるので、
前任の方がいる場合はその就任日から入れておくと、過去の日報の管理者印も正しくなります。

### 10. 夜間の自動確定を始める（任意）

月の締めを自動にするなら、エディタで **`setupNightlyFreeze()`** を1回実行します。
毎晩23時台に見にいって、**7日そろって承認された週があれば自動で確定**します。
確定は週の単位です（帳票が1週 = 1ページの様式のため）。

**★ ここで承認画面がもう一度出ます。**
メールを送る権限（`MailApp`）とトリガーを作る権限（`ScriptApp`）が新しく要るためです。
「許可を確認」→ アカウントを選ぶ →「詳細」→「（安全ではないページ）に移動」→「許可」。
これを通さないと、実行のたびに権限エラーで止まります。

実行すると、こう出ます。

```
夜間の自動確定を始めました。
  毎日 23:00〜24:00 に見にいきます
  （GASの時間主導トリガーは、時刻をこれ以上細かく指定できません）
  一晩に確定する上限: 4 週
  知らせ先: kanri@kurumu.example.jp
```

知らせ先が空欄なら、`CFG.NIGHTLY.NOTIFY` にアドレスを書いてください。

- 様子を見る: **`nightlyStatus()`**
- やめる: **`stopNightlyFreeze()`**

自動にしない場合は、この手順を飛ばしてください。
記録簿で週ごとに［確定］を押す運用になります。

---

## コードを直したあとの注意

`Code.gs` を書き換えて保存しただけでは、**公開中の画面には反映されません。**

**デプロイ → デプロイを管理 → 鉛筆アイコン → バージョン「新バージョン」→ デプロイ**

この操作で反映されます。URLは変わりません。
「新しいデプロイ」を選ぶと別のURLが発行されてしまうので、直すときは必ず
**デプロイを管理** のほうを使ってください。

---

## うまくいかないときは

| 症状 | 原因と対処 |
|---|---|
| 画面が真っ白 | HTMLファイルの名前が `WebApp` になっていない。`WebApp.html` や `webapp` だと開けません |
| 「シートがありません」と出る | `setup()` を実行していない。もう一度実行してください |
| 承認画面から進めない | 「詳細」→「安全ではないページに移動」を経由する必要があります |
| 「担当者が選ばれていません」 | 担当者を1人も登録していない。管理画面から追加してください |
| 承認ボタンが出ない | 管理薬剤師を登録していない、または選択中の担当者が管理薬剤師でない |
| 印影が保存できない | 画像が大きすぎる(8MB超)か、PNG/JPEG以外の形式 |
| 直したのに変わらない | 上の「コードを直したあとの注意」を参照 |

---

## 動作の要点

**入力中の重複防止。** 誰かが数値を入力し始めると、その日は「入力中(氏名)」になり、
他の人の画面では編集できなくなります。閉じ忘れても10分で自動解除されます。
保存は `LockService` で直列化しているので、同時送信でも行が二重になりません。

**管理薬剤師は各時点で1名。** 任期は「就任日」だけを保存し、終了日は次の任期の前日として
毎回計算します。就任日をあとから何度直しても、期間が重なることも隙間ができることも起きません。
各日の日報には、その日に在任していた管理薬剤師が表示されます。

**さかのぼって入力できます。** 週の一覧の上に「前の週／次の週／今週」と日付の指定欄があります。
入力し忘れた日は、その週まで戻って、今日と同じように入力・承認できます。
未来の週は開けません(週を指定しても今週に戻されます)。
過去の週を見ているあいだも、画面いちばん上の「本日の状態」は今日のまま表示されます。

**入力もれの一覧。** 週の一覧の下に「入力もれ」があり、
過去2週間〜半年のあいだで記録のない日を拾って並べます。
日付を押すとその週へ移動し、その日が選ばれた状態で開きます。
保存すると一覧からその日が消えます。今日は(まだ勤務中のこともあるので)対象に入れていません。

**代行承認。** 承認できるのは「その日の管理薬剤師」か「現在の管理薬剤師」です。
現任が過去分を承認した場合、画面には「◯◯ の代行」と表示され、変更履歴にも残ります。

**削除の制限。** 日報に記録がある人、管理薬剤師の履歴がある人は削除できず、退職のみになります。
過去の記録から名前が消えると記録簿として成立しないためです。

**印影。** 画面側で「白背景を透過 → 余白を切り落とし → 300px角」まで正規化してから保存されます。
サーバーは保存だけを担当し、公開URLは一切作りません。
帳票に貼るときは、その列幅と行の高さをシートから読んでセルの中央に置きます。
テンプレートの幅や高さを変えても、貼り位置は中央のままです。

**担当者は空欄から始まります。** 共用の端末で開くことを前提にしているので、
前の人の名前が残らないよう、開くたびに右上の担当欄は空です。
選ばないまま入力を始めると「担当者を選んでください」と促し、保存も止めます。
名前が決まるまで「入力中」のロックもかけないので、
別人の名前でロックがかかって外せない、ということが起きません。

**帳票の出力。** 画面右上のプリンターのアイコンから、期間を指定して出力します。
今週・先週・今月・前月のボタンと、任意の期間指定ができます。

**出力プレビューは、テンプレートシートの実寸を読んで描いています。**
列幅・行の高さ・見出しの文言をシート上で変えれば、プレビューもそのまま追従します。
画面用に別の見た目を持っていないので、プレビューと印刷結果がずれることがありません。

出力は **週単位で小分けに処理** します。GASの実行時間は1回6分までなので、
1年分(52ページ)を一度に処理すると必ず途中で止まります。
画面を開いたまま待っていると、数週ずつ進んで進捗バーが伸びていきます。
印影は処理の最初に人数分だけ読み込んでメモリに持つので、
ページ数が増えてもDriveへのアクセスは増えません。

できあがったPDFは、ドライブの **「業務日報_出力」フォルダ** に保存されます。

---

## 手動で実行できる関数

| 関数 | 用途 |
|---|---|
| `setup()` | 初期構築(1回だけ) |
| `seedForTest()` | テスト用のダミーデータ投入。**本番では実行しないこと** |
| `sweepStaleLocks()` | 期限切れの編集ロックを掃除。1日1回のトリガーに入れておくと安心 |
| `missingDays_(60)` | 過去◯日で記録のない日を返す(画面の「入力もれ」が使っている中身) |
| `buildTemplate()` | 帳票テンプレートを作る(既存は退避してから作り直す) |
| `checkTemplate()` | テンプレートの形を確認し、各日の書き込み先の行番号を表示 |
| `applyTemplateFormats()` | 温度・湿度のセルに単位の表示形式をまとめて設定 |
| `dumpTemplateLayout()` | いまのシートの列幅・行の高さを書き出す(設定に貼れる形) |
| `exportThisWeek()` | 今週分のPDFを出す |
| `exportLastMonth()` | 前月分のPDFを出す |
| `freezeReadyWeeks()` | 確定できる週をまとめて確定する(画面と同じ経路) |
| `setupNightlyFreeze()` | 夜間の自動確定を始める(トリガーを付ける) |
| `stopNightlyFreeze()` | 夜間の自動確定をやめる |
| `nightlyStatus()` | 夜間の自動確定の状態と、いま確定できる月を見る |
| `verifyFixes()` | 確定PDFが確定時のままかを確かめる |
| `updateDatabase()` | **Code.gs を貼り替えたら1回**。シート・列・書式・移行をまとめて通す |
| `version()` | いま貼ってある版と、シートの状態・行の空きを見る |

## 帳票の作り

**1日 = 2行** です。上段に数値と押印、下段は「処方箋枚数」から「管理者印」までを
ひと続きに結合した広い欄で、ここに譲渡記録が入ります。日付だけ2行ぶち抜きです。

見出しは2行。1行目が列名、2行目が譲渡記録の内訳(譲渡区分/譲渡先名/…)です。
全16行(見出し2行 ＋ 7日 × 2行)。

| 列 | 上段 | 下段 |
|---|---|---|
| A | 日付 / 曜日 / 営業時間(2行ぶち抜き) | ← |
| B | 処方箋枚数 | 管理に関する事項(B〜L を結合) |
| C | 「枚」 | ← |
| D | 疑義照会件数 | ← |
| E | 「件」 | ← |
| F | (空) | ← |
| G | 調剤室 温度 | ← |
| H | 調剤室 湿度 | ← |
| I | 冷所 温度 | ← |
| J | 冷所 湿度 | ← |
| K | 担当者印 | ← |
| L | 管理者印 | ← |

**温度と湿度の単位は表示形式で付けます。**

| 項目 | 表示形式 | 見え方 |
|---|---|---|
| 温度 | `0.0"℃　/"` | 23.5℃ / |
| 湿度 | `0"%"` | 46% |

セルの中身は数値のままなので、あとから平均や集計を取れます。
管理基準を外れた値は赤字で印字されます。温度と湿度が別セルなので、外れた側だけが赤くなります。

**記録のない日は、テンプレートの見た目をそのまま残します。** 白紙の帳票として印刷して
手書きで埋める、という使い方もできます。この動きは `CFG.TEMPLATE.KEEP_BLANK_PLACEHOLDER`
で切り替えられます。

## 次段階

- 未提出アラートを新しいDBに向け直し、LINE WORKS へ通知
- ブラウザを閉じても出力が続くよう、継続トリガー方式を足す(いまは画面を開いたまま待つ方式)
